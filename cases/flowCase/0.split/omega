FoamFile { version 2.0; format ascii; class volScalarField; object omega; }
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //
dimensions      [0 0 -1 0 0 0 0];
internalField   uniform 0.1;
boundaryField
{
    inlet
    {
        type            turbulentMixingLengthFrequencyInlet;
        mixingLength    10;              // ~ obstacle-scale length [m]
        value           uniform 0.1;
    }
    outlet      { type inletOutlet; inletValue uniform 0.1; value uniform 0.1; }
    top         { type symmetry; }
    Terrain     { type omegaWallFunction; value uniform 0.1; }
    Buildings   { type omegaWallFunction; value uniform 0.1; }
}
// ************************************************************************* //
